# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Settings
JWT_SECRET=your_super_secret_key_change_in_production
JWT_EXPIRES_IN=1h

# Database Configuration (for MySQL - optional)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=leave_mgmt_db
